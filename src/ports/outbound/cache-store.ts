/**
 * Cache Store Port (Interceptor-level)
 *
 * Simpler cache interface used by the cache interceptor.
 * Re-exports from middleware/types.ts.
 */

export type { CacheStore } from '../../middleware/types.js'
