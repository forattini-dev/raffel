/**
 * Compat re-export — moved to src/server/orchestration/registration.ts
 * as part of slice 8 of the architecture-deepening initiative (PRD #6 / issue #11).
 *
 * New code should import from `server/orchestration/registration.js` directly.
 */

export * from '../server/orchestration/registration.js'
export type * from '../server/orchestration/registration.js'
