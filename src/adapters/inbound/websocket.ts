/**
 * WebSocket Inbound Adapter
 *
 * Translates WebSocket messages into Envelope format for the core Router.
 * @see ../websocket.ts for the implementation.
 */
export { createWebSocketAdapter } from '../websocket.js'
export type { WebSocketAdapter, WebSocketAdapterOptions } from '../websocket.js'
