/**
 * TCP Inbound Adapter
 *
 * Translates TCP connections into handler invocations.
 * @see ../tcp.ts for the implementation.
 */
export { createTcpAdapter, createTcpClient } from '../tcp.js'
export type { TcpAdapter, TcpAdapterOptions } from '../tcp.js'
